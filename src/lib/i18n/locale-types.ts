/** Resolved UI language (formatting + strings). */
export type UiLocale = "en" | "fr";

/** Stored preference: follow browser, or force a language. */
export type LocalePreference = "auto" | "en" | "fr";
